<%= "No articles found" unless @articles.any? %>
<ul>
    <% @articles.each {|article|%>
        <% unless article.archived?%>
            <li><%= link_to article.title , article %></li>
        <% end %>
    <% } %>
</ul>